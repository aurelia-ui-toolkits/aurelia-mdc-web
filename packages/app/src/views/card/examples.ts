import defaultHtml from '!!raw-loader!./default.html';
import defaultSass from '!!raw-loader!./default.scss';

import headlinesHtml from '!!raw-loader!./headlines.html';
import headlinesSass from '!!raw-loader!./headlines.scss';

import photosHtml from '!!raw-loader!./photos.html';
import photosSass from '!!raw-loader!./photos.scss';

import musicHtml from '!!raw-loader!./music.html';
import musicSass from '!!raw-loader!./music.scss';

export class Examples {
  defaultHtml = defaultHtml;
  defaultSass = defaultSass;

  headlinesHtml = headlinesHtml;
  headlinesSass = headlinesSass;

  photosHtml = photosHtml;
  photosSass = photosSass;

  musicHtml = musicHtml;
  musicSass = musicSass;
}
