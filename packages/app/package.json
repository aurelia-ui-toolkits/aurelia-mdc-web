{
  "name": "@aurelia-mdc-web/app",
  "version": "9.1.3-au2",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "webpack serve --progress",
    "prebuild": "rimraf --glob dist/*.{js,css,map,html}",
    "build:app": "npm run prebuild && webpack --progress",
    "analyze": "npm run prebuild && webpack --env production --analyze",
    "test": "echo \"Error: no test specified\" && exit 1",
    "update-versions": "ts-node -P ../../tsconfig.scripts.json ../../scripts/update-versions.ts"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aurelia-mdc-web/all": "9.1.3-au2"
  }
}